import { pathWithRouter as pingPathWithRouter } from "./ping-domain-router";
import { PathWithRouter } from "../../../../api/core/types";

export const domainPathsWithRouters: PathWithRouter[] = [pingPathWithRouter];
